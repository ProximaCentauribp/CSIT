#include <iostream>

using namespace std;

int main()
{
    double a, b, c;
    cin >> a >> b >> c;
    // используем тернарный оператор
    cout << (a + b > c, a + c > b, c + b > a) ? "Да" : "Нет";
}
