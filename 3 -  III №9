#include <iostream>
#include <cmath>
using namespace std;

int main() {
	int a, b;
	cin >> a >> b;
	
	for (b; b >= a; b--) {
		cout << pow(b, 3) << endl;
	}
	
	while (b >= a) {
		cout << pow(b--, 3) << endl;

	}
	
	do
		cout << pow(b--, 3) << endl;
		
	while (b >= a);
	
}
