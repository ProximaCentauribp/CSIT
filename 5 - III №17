#include<iostream>
#include <algorithm>

using namespace std;

int main() {
    int n; cin >> n;
    double s = 0;
    for (int i = 1; i <= n; i++) {
        s += 1. / (pow(5, i) + pow(4, i + 1));
    }
    cout << s;
}
