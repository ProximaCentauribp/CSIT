#include <iostream>
using namespace std;
void f(int *a) {
    if (*a >= 10 && *a <= 99) {
        cout << *a % 10 << *a / 10;
    }
    else 
        cout << *a;
}
int main()
{
    int n; 
    cin >> n;
    f(&n);
}
