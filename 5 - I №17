#include <iostream>
#include <cmath>
using namespace std;

int main() {
	int n; 
	double x, s = 0, b = 1;
	cin >> n;
	cin >> x;
	for (int i = 1; i <= n; i++) {
		b *= cos(x);
		s += b / i;
	}
	cout << s;

}
