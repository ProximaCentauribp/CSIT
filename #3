#include <iostream>
#include <cmath>
#include <algorithm>
#include <iomanip>

using namespace std;

int main()
{
    cout.setf(std::ios_base::boolalpha);
    int a, b, c;
    bool x;
    cin >> a >> b >> c;
    x = a + b > c && a + c > b && c + b > a;
    cout << x;
}
